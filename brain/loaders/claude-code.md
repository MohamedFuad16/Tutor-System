# Claude Code Brain Loader

Bootstrap with `brain:retrieve-semantic`, then inspect the files it returns.

Required gates:

- `npm run brain:impact -- "<target>"`
- `npm run brain:verify`
- `npm run brain:drift-check`

Mutation rule: generated graph, vector, runtime, impact, flow, contract, and snapshot files are regenerated, not hand edited.
