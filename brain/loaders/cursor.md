# Cursor Brain Loader

Open the semantic retrieval result first, then follow dependency-aware files.

Commands:

- `npm run brain:retrieve-semantic -- "<task>"`
- `npm run brain:impact -- "<target>"`
- `npm run brain:query -- state <field>`
- `npm run brain:query -- layouts <route>`

Before commit, run the full brain verification gate.
