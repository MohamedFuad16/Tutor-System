# Aider Brain Loader

Add only retrieved source files to chat. Start with:

```bash
npm run brain:retrieve-semantic -- "<task>"
npm run brain:impact -- "<target>"
```

Use generated context packs to decide which files to edit. After edits, regenerate and verify brain artifacts.
