# Antigravity Brain Loader

Initialize architecture context with semantic retrieval and static/runtime impact.

Do not trust stale context. Run:

```bash
npm run brain:drift-check
```

If stale, regenerate before planning. Do not mutate generated artifacts except through brain commands.
