# Orchestration: Adaptive Learning Brain Architecture

## Execution Rules

- Keep the original objective intact.
- Ask for approval before risky, expensive, external, or destructive actions.
- Keep immediate blocking work local.
- Delegate only bounded, disjoint, materially useful packets.
- Integrate packet results before final verification.

## Branching Rules
- Start A1-A4 in parallel because their ownership is disjoint.
- Continue local work on repository anchors, workflow state, and final document
  structure while A1-A4 run.
- Start A5 synthesis only after A1-A4 are complete or a lane is explicitly
  marked blocked.
- If an agent returns uncited claims for current research, refresh those claims
  with primary sources before accepting.
- If cost estimates conflict, use explicit assumptions and present ranges.
- If architecture claims conflict with current local source, Graphify query the
  relevant symbol and inspect only the connected source files needed.

## Packet Prompts
- See `packets/A1-knowledge-tracing.md`.
- See `packets/A2-dual-layer-interaction.md`.
- See `packets/A3-brain-data-layer.md`.
- See `packets/A4-aws-production.md`.
- See `packets/A5-synthesis.md`.

## Completion Audit
- All packet deliverables checked.
- Source links included for current research and cloud-service facts.
- Data schemas cross-reference correctly.
- User isolation is present in every data store and query pattern.
- Brain update path is asynchronous and non-blocking.
- Context Builder has a concrete token budget and compression policy.
- Roadmap, risks, and open questions are complete.
